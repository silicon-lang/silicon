extern fn printf(format: string, ...): i32;

fn fibonacci(x: i64): i64 {
  if (x <= 2) return 1;

  return fibonacci(x - 1) + fibonacci(x - 2);
}

fn main() {
  printf("%lli\n", fibonacci(40));

  return 0;
}
